# 手动动作规范

手动与辅助操作页右侧那 26 个按钮怎么工作。版面见 `B-Manual-手动与辅助操作.html`，
实现见 `src/RollGrinder.Services/Manual/`，规则由
`tests/RollGrinder.Integration.Tests/ManualCommandTests.cs` 守住。

---

## 一、26 个按钮全部是**离散命令**，没有"按住才动"

| 组 | 个数 | 动作 |
|---|---|---|
| 测量臂 | 8 | A 放下 / A 收起 / B 放下 / B 收起 / AB 到轧辊 / AB 归位 / 校测量臂 / 测量采样 |
| 尾架与套筒 | 6 | 套筒伸出 / 套筒缩回 / 尾架向前 / 尾架向后 / 尾架夹紧 / 尾架放松 |
| 头架、中心架与其他 | 12 | 头架启动 / 头架 + / 头架 − / 拨盘伸出 / 拨盘收回 / U1 轴归零 / 软着陆升 / 软着陆降 / 冷却水 / 砂轮启动 / 测砂轮直径 / 各轴归位 |

**这里没有点动（hold-to-run）。** 点动属于实时控制回路，需要硬件使能与安全回路托底，
归机床操作面板与 PLC——上位机被强制结束时，按住的那个键就松不开了，
这正是最高原则要避开的事。

所以「头架 +」「头架 −」是**转速加减一档**，不是把主轴点着转；
「软着陆升／降」是**发一个到位命令**，不是按住才升。

---

## 二、三种命令形式

| 形式 | 怎么写 | 用在哪 |
|---|---|---|
| **脉冲 Pulse** | 写 `true` → 等一个脉宽（`hmi.json` 的 `manualPulseMs`，默认 300 ms）→ 写 `false` | 22 个动作 |
| **保持 Toggle** | 按开关写 `true` / `false`，状态可回读 | 冷却水、砂轮启动、头架启动 |
| **本地 Local** | 不写机床 | 测量采样（走测量服务存一个测点） |

> ### PLC 侧必须按**上升沿**触发并自行复位
>
> 上位机在写完 `true`、还没写 `false` 时被强制结束，那一句 `false` 就发不出去了。
> 脉冲的收尾是**尽力而为**，PLC 的边沿触发与自复位才是最后一道保险。
> tagmap 样例里每个命令位的 `description` 都写了这一条。

---

## 三、四道门禁

按钮按下去之前依次问四件事，任何一条不过都**一个字节也不写**，并且说清楚原因：

| 检查 | 不过时 |
|---|---|
| tagmap 里登记了这个命令位吗 | 按钮**压暗**（留在原位，不跳动），ToolTip 说明缺映射 |
| 连上机床了吗 | 禁用 + 报警"没连上机床" |
| 自动循环还挂着程序吗 | 禁用 + 报警；**读不到通道状态一律当作在忙**——不知道机床在干什么就别乱动它 |
| 是危险动作吗 | 第一下只变红显示"再按一次"，4 秒内按第二下才真发 |

**这四道是防呆，不是安全功能。** 真正的联锁在 PLC，上位机只是不去按那个按钮。

### 运行中唯一放行的两个

- **冷却水** —— 磨削当中开关冷却水是正当操作
- **测量采样** —— 不写机床

其余 24 个在 `通道状态 ≠ 复位` 时一律禁用。

### 要按两次的四个

尾架放松 / 拨盘收回 / 软着陆降 / 各轴归位。

理由分别是：让几十吨的辊子失去一端支承、轧辊没停稳就收拨盘、把辊子落到托瓦上、
整台机床一起动。同一时刻只留**一个**待确认的按钮，免得两个红按钮并排让人按错；
4 秒内不按第二下自动撤销，免得一小时后的误触被当成确认。

---

## 四、按钮的四种样子

| 样子 | 含义 |
|---|---|
| 白底描边 | 可按 |
| 压暗 35% | tagmap 没登记这个动作 |
| 变灰不可按 | 机床在忙 / 没连上，ToolTip 说明原因 |
| **红底「再按一次」** | 危险动作等第二下 |
| **绿底** | 保持型动作正开着（按状态回读点亮，读不到就不亮，不假装关着） |

动作发出去之后，按钮矩阵上方闪一条「XXX · 已发出」，停 3 秒自己消失——
**不占报警条**，报警条留给真正的故障。

---

## 五、配置

### tagmap.json

每个动作一个布尔命令位，逻辑名 `manual.<动作键>`；保持型动作另有
`manual.<动作键>.state` 用于状态回读。样例把 25 个命令位排在
`"HMI_Cmd"` 的 bit0–bit24，3 个状态位排在 `"HMI_State"`——
**现场按自己的 DB 块改地址，逻辑名不要动**。

没登记的动作按钮会压暗：**先接哪个就先映射哪个，不用一次全接**。

### hmi.json

```
manualPulseMs   脉冲宽度（ms），默认 300
```

### 仿真

仿真机床把 `manual.X` 的布尔写入原样回显到 `manual.X.state`，
所以三个保持型按钮在仿真里是真的会亮的——仿真机床就是这台"机床"，这不是假数据。

---

## 六、代码落点

| 关注点 | 位置 |
|---|---|
| 26 个动作的目录（形式、门禁、是否要确认） | `Services/Manual/ManualCommand.cs` |
| 门禁与写入（界面不碰网关，架构约束 ②⑦） | `Services/Manual/ManualCommandService.cs` |
| 按钮状态与二次确认 | `App/ViewModels/ManualViewModel.cs` |
| 按钮四种样子 | `App/Themes/Controls.xaml` 的 `MachineActionTemplate` |
| 逻辑名 | `Contracts/MachineTagKeys.cs` 的 `ManualCommand` / `ManualCommandState` |

新增一个动作 = 在目录里加一行 + resx 加一条文案 + tagmap 加一个命令位。
界面、门禁、确认逻辑都不用改。
