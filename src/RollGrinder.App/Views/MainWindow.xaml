<Window x:Class="RollGrinder.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:RollGrinder.App.Localization"
        Title="{loc:Loc App_Title}"
        Height="480"
        Width="760"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{loc:Loc Main_Header}"
                   FontSize="22"
                   Margin="0,0,0,16" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="{loc:Loc Main_MachineLabel}" Margin="0,0,16,6" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding MachineDisplayName}" Margin="0,0,0,6" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="{loc:Loc Main_GatewayLabel}" Margin="0,0,16,6" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding GatewayName}" Margin="0,0,0,6" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="{loc:Loc Main_TagCountLabel}" Margin="0,0,16,6" />
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TagCount}" Margin="0,0,0,6" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="{loc:Loc Main_ConfigDirectoryLabel}" Margin="0,0,16,6" />
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ConfigDirectory}" Margin="0,0,0,6" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="{loc:Loc Main_DataDirectoryLabel}" Margin="0,0,16,6" />
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding DataDirectory}" Margin="0,0,0,6" />

            <TextBlock Grid.Row="5" Grid.Column="0" Text="{loc:Loc Main_LogDirectoryLabel}" Margin="0,0,16,6" />
            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding LogDirectory}" Margin="0,0,0,6" />
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="{loc:Loc Main_SkeletonNotice}"
                   VerticalAlignment="Bottom"
                   Opacity="0.7"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
